// import util.*; // Removed as it cannot be resolved

public class avg3 {
    public static int avg(int a, int b, int c) {
        return (a + b + c) / 3;
    }
    public static void main(String[] args) {
        int a=1;
        int b=2;
        int c=3;
        System.out.println(avg(a, b, c));

    }
}